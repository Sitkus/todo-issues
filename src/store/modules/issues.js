const state = {
  issues: [
    { name: 'Banana', price: 20 },
    { name: 'Orange', price: 30 }
  ]
};

const getters = {
  allIssues: (state) => state.issues
};

const actions = {
  // Add
};

const mutations = {
  // Add
};

export default {
  state,
  getters,
  actions,
  mutations
};
